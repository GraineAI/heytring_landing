Video assets (generated brand films, recorded from CSS-animated scenes):
  hero-loop.webm / hero-loop-portrait.webm — hero ambient film (12s loop, 16:9 + 9:16)
  demo.webm — the product demo film (~15s) behind the "Play the demo" facade
  *-poster.jpg — instant-paint poster frames
Swap any of them by replacing the file (same name); the site needs no code change.
